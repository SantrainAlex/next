name: Workflow push
on: push
jobs:
  test:
    name: Mon premier test
    runs-on: ubuntu-latest
    steps:
      - name: récupérer le code
        uses: actions/checkout@v4
      - name: install
        run: npm install
      - name: build
        run: npm run build

